appId: ${HAPPIER_E2E_MOBILE_APP_ID}
---
- assertVisible:
    id: main-header-start-new-session
- tapOn:
    id: main-header-start-new-session

- extendedWaitUntil:
    visible:
      id: new-session-composer-input
    timeout: 60000
